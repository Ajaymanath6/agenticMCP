{
  "name": "agentic-mcp-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p mcp-server/tsconfig.json",
    "vercel-build": "echo '--- STEP 1: Starting vercel-build script. ---' && node mcp-server/scripts/copy-blueprints.mjs && echo '--- STEP 2: Copy finished. Now running build. ---' && npm run build && echo '--- STEP 3: Vercel build complete. ---'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "@vercel/node": "^5.5.16",
    "typescript": "^5.9.3"
  }
}
